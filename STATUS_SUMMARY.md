# 工作进度总结（2026-06-27 会话）

## 文件信息
- 主文件：`/Users/wuyangcj/trae/回甘demo/回甘—考研数学智题本.html`（约3.2MB单文件Web应用）
- 备份文件：`回甘—考研数学智题本.html.bak2`（2026-06-26 19:59，3186384字节）
- 损坏备份：`回甘—考研数学智题本.html.corrupted`（被错误脚本破坏，勿用）

## 当前状态：知识库小于号修复已完成

### 已完成的修复（本次会话）
知识库详情（行43951-44330）中的数学小于号 `<` 被浏览器误解析为HTML标签的问题，共修复 **12处**：

**小于号替换（9处）：**
| 行号 | 原始内容 | 修复后 |
|------|----------|--------|
| 44033 | `r(A)=r(A\|b)<n` | `r(A)=r(A\|b) \lt n` |
| 44033 | `r(A)=r<n` | `r(A)=r \lt n` |
| 44216 | `\|x\|<R` | `\|x\| \lt R` |
| 44248 | `r(A)<n` | `r(A) \lt n` |
| 44248 | `r(A)<r(A\|b)` | `r(A) \lt r(A\|b)` |
| 44258 | `r(A)=r(A\|b)<n` | `r(A)=r(A\|b) \lt n` |
| 44258 | `r(A)<r(A\|b)` | `r(A) \lt r(A\|b)` |
| 44305 | `0<x<1` | `0 \lt x \lt 1` |
| 44315 | `0<x<1` | `0 \lt x \lt 1` |

**双空格修复（3处，之前 `<` 被误删除留下的双空格）：**
| 行号 | 原始内容 | 修复后 |
|------|----------|--------|
| 44033 | `r(A)  n` | `r(A) \lt n` |
| 44033 | `r(A)  r(A\|b)` | `r(A) \lt r(A\|b)` |
| 44038 | `r(A)  m` | `r(A) \lt m` |

### 修复脚本
- `fix_kb_lt_v3.py`：知识库小于号修复脚本（已验证正确）
  - 关键逻辑：替换 `<` 为 `\lt`，前后自动加空格
  - 双空格修复：从后往前替换避免位置偏移
  - 排除HTML标签、`\(...\)` 数学分隔符、`<code>` 标签内的内容

### 未完成的工作（待继续）

#### 1. 题库中小于号问题（未修复）
题库数据中存在大量数学小于号被误解析的问题，分布在：
- 行3850、3927、3929、3945、4116、41608、41623、41640、4274、4366、4376
- 行20484、21123、21130、22800、22823、22831、22837、23018
- 行42200-42813（约20组重复题目）

**重要教训**：之前用 `fix_question_bank_lt.py` 脚本修复题库时，该脚本破坏了JS代码（把正则表达式中的 `<oli>`、`<uli>` 等也替换了），导致文件损坏。已从 bak2 恢复。

**正确做法**：题库修复需要更精确的脚本，只针对题目数据字段（content、options、analysis、answer、hint、formula）进行修复，避免触碰JS逻辑代码。

#### 2. 用户数据与项目代码分离存储（待检查）

#### 3. 自建题库导入功能完善（待自测）
- 已删除手动增加功能，保留导入功能
- 支持 JSON/TXT/Excel 格式导入
- 需要自测与做题功能、删除功能的衔接
- 相关函数位置：
  - `triggerImport(format)` - 行47262
  - `handleImportFile(event)` - 行47322
  - `parseJSONQuestions(text)` - 行47357
  - `parseTXTQuestions(text)` - 行47390
  - `parseExcelQuestions(arrayBuffer)` - 行47419
  - `showImportPreview(questions)` - 行47451
  - `confirmImport()` - 行47486

#### 4. 免费云端部署方案（待提供）

#### 5. 后端账号容量评估（待提供）

## 关键技术约束
1. LaTeX命令在JS字符串中存储为双反斜杠（如 `\\frac`、`\\lt`）
2. Python脚本中用 `r'\\lt'` 或 `'\\\\lt'` 匹配文件中的 `\\lt`
3. MathJax配置不支持 `$...$` 定界符，行内用 `\(...\)`，块级用 `$$...$$`
4. `wrapMathInHtmlText` 函数（行47676）不转义 `<`，导致数学小于号被误解析
5. 修复时不能触碰JS逻辑代码中的 `<`（如正则表达式、HTML字符串操作）

## 验证方法
- 用 Grep 搜索 `r(A)<n`、`|x|<R` 等模式确认已修复
- 用 `check_lir.py` 检查 `<lir(A)` 等HTML标签被破坏的问题
- 最终需要用 Playwright 自动化检测验证渲染效果
